# Shared base for every spec in this repository (nushell).
#
# NOT a spec: it has no name/target/source and is never loaded on its own.
# Everything a package owns — name, target, source, assets, asset_type,
# metadata, catalog, tests, versions, announce, annotations — stays in that
# package's own mirror.yml.
#
# ⚠️ `extends:` is a SHALLOW merge of top-level keys. A spec that sets a key
# below replaces that block OUTRIGHT — there is no per-key merge. `platforms:`
# is where this bites: it carries BOTH the runner matrix and the container
# legs, so a child that restates it to tweak one runner silently drops every
# `containers:` entry. Nothing reds — the legs simply stop existing, and every
# os.features claim in that spec goes back to being merely asserted. Restate
# `platforms:` IN FULL or not at all.

skip_prereleases: true
cascade: true

# The fleet rule. A cascade tag (`latest`, `X`, `X.Y`) re-points on every
# re-publish; without a build segment the new manifest reuses the bare version
# tag and orphans the prior digest for registry GC, breaking any `@sha256:` pin
# in a consumer's ocx.lock. Nushell versions are bare `MAJOR.MINOR.PATCH`, so
# there is no existing +build segment for `date` to collide with — the `none`
# exception (upstream versions that already carry `+YYYYMMDD`, where `date`
# empties the mirror) does not apply here. This replaces the flat layout's
# `build_timestamp: none`, which predates the fleet rule and which the 0.5.0
# toolchain warns about at spec load; nothing is published at the new
# coordinate yet, so the change costs no orphaned digest.
build_timestamp: date

verify:
  github_asset_digest: true

concurrency:
  # Assets are ~40 MB tarballs (the Windows zip is ~180 MB unpacked — the
  # `nu_plugin_polars` binary alone is 88 MB), so bundling stays narrow.
  max_downloads: 8
  max_bundles: 4
  max_pushes: 2
  rate_limit_ms: 100
  max_retries: 3
  compression_threads: 0

# `off`, and it cannot be otherwise here. The scan only looks BELOW an
# interface PATH entry of the form ${installPath}/<dir>, and this bundle has
# none — the archive's content root IS the bin dir once the versioned wrapper
# is stripped, so PATH is a bare ${installPath}. `verify` is rejected at spec
# load (measured, exit 65):
#
#   ERROR invalid mirror spec:
#     - bin_scan: bin_scan is enabled but metadata.json declares no
#       interface-visible ${installPath}/<dir> PATH entry — the verification
#       would inspect no file and pass green whatever the archive contains.
#
# `off` with a hand-listed `binaries` in metadata.json is the blessed shape;
# see the `binaries` note in nushell/mirror.yml for what that list claims and
# what it does not. A future package here that ships a real bin/ dir should
# override this to `verify`.
bin_scan: off

# ─── Platform matrix ────────────────────────────────────────────────────────
#
# `os.features` states what an artifact REQUIRES OF THE HOST — it is not a
# label for how it was built. This mirror selects the `-unknown-linux-musl`
# tarballs, and measured on 0.114.1 both are fully static: no PT_INTERP, no
# DT_NEEDED (evidence above nushell/mirror.yml's `assets:`). No requirement on
# the host means both Linux keys are BARE. Tagging them `+libc.musl` would be
# a false requirement that hid them from every glibc host.
#
# A bare key claims universality, so it must prove it: ubuntu (glibc) + alpine
# (musl) + fedora (different glibc vintage). The alpine leg IS the proof —
# drop it and the universal claim is merely asserted.
#
# A future package here whose libc measurement differs must restate this whole
# block in its own spec — see the shallow-merge warning at the top.
platforms:
  linux/amd64:
    runner: ubuntu-latest
    containers:
      - { image: "ubuntu:24.04", shell: bash }
      - { image: "alpine:3.20",  shell: sh }
      - { image: "fedora:40",    shell: bash }
  linux/arm64:
    # arm64 container legs run natively — no qemu — so the runner arch must
    # match the platform or the renderer reds the leg up front.
    runner: ubuntu-24.04-arm
    containers:
      - { image: "ubuntu:24.04", shell: bash }
      - { image: "alpine:3.20",  shell: sh }
      - { image: "fedora:40",    shell: bash }
  darwin/amd64:
    # Intel slice tested on the arm64 macos-14 runner via Rosetta 2 — GitHub's
    # native macos-13 Intel pool is exhausted and deadlocks the push job.
    runner: macos-14
    prefix: ["arch", "-x86_64"]
  darwin/arm64:
    runner: macos-14
    shell: bash
  windows/amd64:
    runner: windows-latest
    shell: pwsh
  windows/arm64:
    # Native arm64 Windows runner — the amd64 pool cannot execute an ARM64 PE.
    runner: windows-11-arm
    shell: pwsh
  # No `containers:` on darwin or windows — those runners have no Linux
  # container engine, and the key is rejected at spec load.

notify:
  discord:
    webhook_secret: OCX_MIRROR_DISCORD_HOOK
    user_id: "227748867204972545"  # pinged in-message on failures
